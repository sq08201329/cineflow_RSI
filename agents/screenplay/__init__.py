"""剧本 Agent 降级模式包（功能 009）：剧本产出全量记录 + 人工改策略闭环。

004/006/007/008 模式的第五次同构，但**治理面最重**：剧本环节评估信号过弱（判据数据
未积累），故按宪章原则六以"记录-回放 + 人工改策略"降级模式接入——**禁止 dreaming 为
本 Agent 生成候选策略**（配置化名单 + 显式拒绝），策略仅由人工编写与提交：
题材/约束 + 目标时长 + 角色设定 → 分阶段产出（outline → scenes → script，各自独立
节点与评估）→ 七评估器（四门禁 + 两代理 + judge 仅大纲阶段）→ 定点归一 → 一次性
INSERT 落树；人工策略改动经静态检查（002）与版本化（BLAKE3 前 12 位 + 谱系）后在
模拟器池上回放对比，**人工采纳**才更新部署指针；升级判据材料持续积累（阈值配置化 +
自动结论 + 推翻留痕），升级本身须另立决议。

本包是全线唯一不含自动进化的闭环；其骨架（降级模式）是开发 Agent 的复用对象。
"""
