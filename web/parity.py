"""同源比对辅助（测试用）：接口响应 vs 既有 JSON 报告逐字段一致（FR-010 分层口径）。

分层（2026-09-21 澄清决议）：

- **有报告产物的面板**——进化曲线（dreaming/history 轮次报告）、信度（010 报告）、
  漂移（012 报表）、谱系（005 谱系报表）：接口响应与报告逐字段比对，输出差异报告；
- **树/节点接口**：以 DB 只读读数为权威源（字段语义与 001 落库 schema 一致，
  无第二口径可比）——比对方式是字段集/类型与落库列对齐断言。

本模块只做比对（纯函数，零 import core/agents/dreaming，不读写任何东西），供
`tests/contract/test_web_parity.py` 调用；服务与导出不依赖它。
"""
